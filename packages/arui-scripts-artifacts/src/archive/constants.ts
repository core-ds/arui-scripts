/** Имя tar-архива по умолчанию. */
export const DEFAULT_ARCHIVE_NAME = 'build.tar';

/** Имя временной директории, в которой собирается содержимое tar-архива. */
export const DEFAULT_ARCHIVE_TEMP_DIR_NAME = '.archive-build';

/** Директории проекта, которые кладутся в архив рядом со сборкой. */
export const DEFAULT_ARCHIVE_ADDITIONAL_PATHS = ['config'];
