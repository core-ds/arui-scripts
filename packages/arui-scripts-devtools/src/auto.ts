/**
 * Энтрипоинт автоинжекта: arui-scripts подмешивает этот файл в основной клиентский бандл,
 * когда включён ключ `devtools`. Ничего не экспортирует и всё делает сам —
 * вешает хоткей и бейдж, а код панели грузит отдельным чанком только при открытии.
 */
import { installDevtools } from './install';

installDevtools();
